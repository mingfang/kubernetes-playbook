---
# defaults file for deployment

role_name: "{{role_path|basename}}"
resource_type: "{{role_name}}"
resources: "{{ namespace.services|selectattr('pod', 'defined')|selectattr('stateful', 'undefined')|list + namespace.services|selectattr('pod', 'defined')|selectattr('stateful', 'defined')|rejectattr('stateful')|list}} "

