{
    "kind": "Role",
    "apiVersion": "rbac.authorization.k8s.io/v1",
    "metadata": {
        "name": "{{ item.name }}",
        "namespace": "{{ namespace.name }}",
        "labels": {
            "managed": "true"
        }
    },
    "rules": {{ item.rules | to_json }}
}
