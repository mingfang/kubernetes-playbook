{
    "kind": "HorizontalPodAutoscaler",
    "apiVersion": "autoscaling/v1",
    "metadata": {
        "name": "{{item.name}}"
    },
    "spec": {
        "scaleTargetRef": {
            "kind": "Deployment",
            "name": "{{item.name}}"
        },
        "minReplicas": {{item.autoscale.min|default(1)}},
        "maxReplicas": {{item.autoscale.max|default(1)}},
        "targetCPUUtilizationPercentage": {{item.autoscale.cpu|default(80)}} 
    }
}
